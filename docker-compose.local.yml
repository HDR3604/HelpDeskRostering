name: help-desk-rostering
services:
  backend:
    build:
      context: ./apps/backend
      dockerfile: Dockerfile.dev
    ports:
      - "8080:8080"
    volumes:
      - ./apps/backend:/app
      - /app/tmp  # Exclude tmp dir from mount
    env_file:
      - .env.local
    environment:
      - DATABASE_URL=postgres://helpdesk:helpdesk_local@postgres:5432/helpdesk?sslmode=disable
    depends_on:
      postgres:
        condition: service_healthy
      scheduler:
        condition: service_healthy
    restart: unless-stopped

  scheduler:
    build:
      context: ./apps/scheduler
      dockerfile: Dockerfile.dev
    expose:
      - "8000"
    volumes:
      - ./apps/scheduler:/app
      - /app/tmp  # Exclude tmp dir from mount
    environment:
      - LOG_LEVEL=DEBUG
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/v1/healthy')"]
      interval: 10s
      timeout: 5s
      retries: 3

  transcript-extraction:
    build:
      context: ./apps/transcript_extraction_function
      dockerfile: Dockerfile.dev
    expose:
      - "8001"
    volumes:
      - ./apps/transcript_extraction_function:/app
      - /app/tmp  # Exclude tmp dir from mount
    environment:
      - LOG_LEVEL=DEBUG
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8001/api/v1/healthy')"]
      interval: 10s
      timeout: 5s
      retries: 3

  frontend:
    build:
      context: ./apps/frontend
      dockerfile: Dockerfile.dev
    ports:
      - "5173:5173"
    volumes:
      - ./apps/frontend:/app
      - /app/node_modules  # Exclude node_modules from mount
    restart: unless-stopped

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: helpdesk
      POSTGRES_PASSWORD: helpdesk_local
      POSTGRES_DB: helpdesk
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U helpdesk -d helpdesk"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
