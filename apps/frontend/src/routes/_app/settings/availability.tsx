import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_app/settings/availability')({
    component: () => <p className="text-muted-foreground">Coming soon.</p>,
})
